/**
 * SearchOptions - Options for the search use case
 * Empty for now. Fields added here automatically change the cache key
 * via buildOptionsHash in CachedSearchService.
 */

export interface SearchOptions {
  // future fields: includeForks?: boolean, minRepos?: number, etc.
}
